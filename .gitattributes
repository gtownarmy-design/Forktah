# The preserved gateway bytecode is the ONLY surviving copy of a lost source file
# (see taskmgmt/recovered/README.md). Git's auto-detection gets this right today,
# but this checkout is on a Windows drive and a core.autocrlf setting on some other
# machine would rewrite line endings inside the file and silently corrupt it - and a
# corrupt .pyc cannot be recompiled, because there is no source. Pin it explicitly.
taskmgmt/recovered/*.bin binary
*.pyc                    binary

# Windows launchers are read by cmd.exe and wscript, which expect CRLF. Pinned so a
# checkout made without core.autocrlf (a WSL clone, CI) still produces runnable files.
*.cmd                    text eol=crlf
*.vbs                    text eol=crlf

# Shell scripts are read by bash, which chokes on CR. The suites already run themselves
# through `tr -d '\r'`, but they SOURCE dashboard/testlib.sh directly, so on a Windows
# checkout with core.autocrlf=true every shell suite died with "finish: command not
# found" and the full gate reported 12 suites failed that had never run a check.
# Pinned LF so this checkout can run its own gate.
*.sh                     text eol=lf
